import React from 'react';
import './Footer.module.css';

const Footer = (props) => {
  return (
    <footer>
      <p> MetaBuild Submission Hackathon Aurora</p>
    </footer>
  );
};

export default Footer;
